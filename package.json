{
  "name": "function-of-babel",
  "private": true,
  "packageManager": "bun@1.3.9",
  "scripts": {
    "dev": "rsbuild dev",
    "build": "rsbuild build",
    "preview": "rsbuild preview",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@rsbuild/core": "^2.0.15"
  }
}
